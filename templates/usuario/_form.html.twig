{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <main class="container mt-5">
        {% form_theme form with 'bootstrap_4_layout.html.twig' %}
        {{ form_start(form, {"attr": {"novalidate": "novalidate", "id": "form"}}) }}
        {{ form_widget(form) }}
        <br>
        <input class="btn btn-success btn-sm" type="submit" value="Salvar" id='btnsubmit'>
        <a href="{{ path('usuario_index') }}" class="btn btn-primary btn-sm">Voltar</a>
        {{ form_end(form) }}
    </main>
{% endblock %}
